pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'pilot-root'

include 'pilot-admin',
        'pilot-core',
        'pilot-batch',
        'pilot-test-fixture'

