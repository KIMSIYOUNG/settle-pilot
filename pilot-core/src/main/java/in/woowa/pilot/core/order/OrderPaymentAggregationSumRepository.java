package in.woowa.pilot.core.order;

import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderPaymentAggregationSumRepository extends JpaRepository<OrderPaymentAggregationSum, Long> {
}
